"use client";

import { FormEvent, useState } from "react";
import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, Eye, EyeOff, LoaderCircle, LockKeyhole, ShieldCheck } from "lucide-react";
import { toast } from "sonner";
import { BuaMark, PoweredBy } from "@/components/brand";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function PortalLogin({ mode }: { mode: "staff" | "admin" }) {
  const [staffId, setStaffId] = useState(""); const [password, setPassword] = useState(""); const [show, setShow] = useState(false); const [loading, setLoading] = useState(false);
  const submit = async (event: FormEvent) => { event.preventDefault(); setLoading(true); try { const response = await fetch("/api/auth/login", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ staffId, password, mode }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "Sign-in failed."); window.location.assign(result.redirectTo); } catch (error) { toast.error(error instanceof Error ? error.message : "Sign-in failed."); setLoading(false); } };
  return <main className="min-h-screen bg-[#f3f1ec] p-4 sm:p-7"><div className="mx-auto grid min-h-[calc(100vh-2rem)] max-w-[1300px] overflow-hidden rounded-[30px] border border-[#ddd7cd] bg-white shadow-[0_30px_100px_rgba(26,30,36,.14)] sm:min-h-[calc(100vh-3.5rem)] lg:grid-cols-[1.05fr_.95fr]"><div className="relative hidden overflow-hidden lg:block"><Image src={mode === "admin" ? "/outreach/outreach-3.webp" : "/outreach/outreach-4.webp"} alt="Zendale corporate health outreach team" fill priority className="object-cover"/><div className="absolute inset-0 bg-[linear-gradient(180deg,rgba(15,24,39,.06)_30%,rgba(15,24,39,.88)_100%)]"/><div className="absolute inset-x-0 bottom-0 p-10 text-white"><p className="text-xs font-black uppercase tracking-[.16em] text-[#f3d66a]">Authorised access only</p><h2 className="mt-3 max-w-xl text-4xl font-black leading-tight tracking-[-.05em]">A focused workspace for secure outreach operations.</h2><p className="mt-4 max-w-lg text-sm leading-7 text-white/66">Participant health information is restricted to approved staff and recorded access controls.</p></div></div><div className="flex flex-col p-6 sm:p-10 lg:p-12"><div className="flex items-center justify-between gap-4"><BuaMark/><Button asChild variant="ghost" size="sm"><Link href="/"><ArrowLeft/> Public registration</Link></Button></div><div className="my-auto py-12"><div className="mx-auto max-w-md"><div className="grid size-12 place-items-center rounded-2xl bg-[#17243a] text-white"><LockKeyhole className="size-5"/></div><p className="mt-7 text-xs font-black uppercase tracking-[.15em] text-[var(--bua-red)]">{mode === "admin" ? "Administrator portal" : "Staff portal"}</p><h1 className="mt-2 text-3xl font-black tracking-[-.045em] text-[#1d2229]">Sign in to continue</h1><p className="mt-3 text-sm leading-6 text-[#707781]">Use your immutable Staff ID and password. Public account registration is not available.</p><form onSubmit={submit} className="mt-8 space-y-5"><div className="space-y-2.5"><Label htmlFor="staff-id">Staff ID</Label><Input id="staff-id" value={staffId} onChange={(e) => setStaffId(e.target.value.toUpperCase())} placeholder="BUA-STF-001" autoComplete="username"/></div><div className="space-y-2.5"><Label htmlFor="password">Password</Label><div className="relative"><Input id="password" type={show ? "text" : "password"} value={password} onChange={(e) => setPassword(e.target.value)} className="pr-12" autoComplete="current-password"/><button type="button" onClick={() => setShow((v) => !v)} className="absolute right-2 top-1/2 grid size-9 -translate-y-1/2 place-items-center rounded-lg text-[#69717b] hover:bg-[#efede8]" aria-label={show ? "Hide password" : "Show password"}>{show ? <EyeOff className="size-4"/> : <Eye className="size-4"/>}</button></div></div><Button type="submit" size="lg" className="w-full" disabled={loading}>{loading ? <><LoaderCircle className="animate-spin"/> Signing in securely…</> : <>Sign in</>}</Button></form><div className="mt-5 flex items-center gap-2 rounded-xl bg-[#f4f7fb] px-3.5 py-3 text-xs leading-5 text-[#52657a]"><ShieldCheck className="size-4 shrink-0 text-[var(--zendale-blue)]"/> Repeated failed attempts may temporarily lock access.</div></div></div><PoweredBy/></div></div></main>;
}
